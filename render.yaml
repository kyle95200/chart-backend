services:
  - type: web
    name: chart-backend
    env: python
    plan: free  # or starter / standard
    buildCommand: pip install -r chart-backend/requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    autoDeploy: true
